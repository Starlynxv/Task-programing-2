<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ITLA CRUD - Product Management</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #1a1a1a; color: #e0e0e0; display: flex; flex-direction: column; align-items: center; padding: 50px; }
        .container { background: #2d2d2d; padding: 2rem; border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.5); width: 400px; }
        input { width: 90%; padding: 10px; margin: 10px 0; border-radius: 4px; border: 1px solid #444; background: #3d3d3d; color: white; }
        button { width: 100%; padding: 10px; background: #6200ee; color: white; border: none; border-radius: 4px; cursor: pointer; font-weight: bold; }
        button:hover { background: #7722ff; }
        ul { width: 400px; list-style: none; padding: 0; margin-top: 20px; }
        li { background: #333; margin-bottom: 10px; padding: 15px; border-radius: 4px; display: flex; justify-content: space-between; align-items: center; border-left: 4px solid #6200ee; }
        .delete-btn { width: auto; background: #cf6679; padding: 5px 10px; }
    </style>
</head>
<body>

    <div class="container">
        <h2>Add New Product</h2>
        <input type="text" id="name" placeholder="Product Name">
        <input type="number" id="price" placeholder="Price">
        <button onclick="addProduct()">Save Product</button>
    </div>

    <ul id="productList"></ul>

    <script>
        
        const API_URL = "https://localhost:7149/api/products";

        async function fetchProducts() {
            const response = await fetch(API_URL);
            const data = await response.json();
            const list = document.getElementById('productList');
            list.innerHTML = '';
            data.forEach(p => {
                list.innerHTML += `
                    <li>
                        <span><strong>${p.name}</strong> - $${p.price}</span>
                        <button class="delete-btn" onclick="deleteProduct(${p.id})">Delete</button>
                    </li>
                `;
            });
        }

        async function addProduct() {
            const name = document.getElementById('name').value;
            const price = document.getElementById('price').value;
            await fetch(API_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ name, price: parseFloat(price) })
            });
            fetchProducts();
        }

        async function deleteProduct(id) {
            await fetch(`${API_URL}/${id}`, { method: 'DELETE' });
            fetchProducts();
        }

        // Carga inicial
        fetchProducts();
    </script>
</body>
</html>